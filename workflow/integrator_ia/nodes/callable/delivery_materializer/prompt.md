Read only the `snapshot_path` values and hashes pinned by the source inventory;
do not reread moving donor worktrees. Verify every selected manifest hash
before porting. Apply kernel changes first, then merge
PISA into overlapping attention/block files, then add cache at the denoiser
boundary. Preserve all earlier changes during each semantic port.

Expose kernel, PISA, and cache through independent runtime guards. All three
implementations must be present in the integrated source, but no delivery
recipe is required to enable all three simultaneously.

Record every source and destination file in `INTEGRATION-SOURCES.lock.json`
with source hash, destination hash, and merge mode, including files that are
already equivalent in the integration baseline. Never use a donor workflow
module as a runtime dependency and never modify donor files.
