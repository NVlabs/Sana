id = "ltx23_baseline"
kind = "baseline"
purpose = "control"
description = "LTX-2.3 22B 1088x1920 241f two-stage HQ baseline; official pipeline on a single GB200 GPU with every acceleration seam explicitly off. Control arm for the optimized stack."
model_profile = "ltx23"

[runtime]
root = "models/ltx23/baseline"

[artifacts]
output_dir = "outputs"
video = "out.mp4"
log = "run.log"
benchmark = "perf.json"
run_config = "run_config.json"
