schema_version = 1
id = "wan22_t2v_a14b"
display_name = "Wan2.2-T2V-A14B 720p81 T2V pristine baseline (diffusers WanPipeline, MoE)"
legacy_profile = "models/wan22_t2v_a14b.toml"
contract_kind = "model_baseline_minimal_copy"

[baseline]
manifest = "candidates/wan22_t2v_a14b_baseline.toml"
runtime_root = "models/wan22_t2v_a14b/baseline"
eval_profile = "evals/profiles/official_video_t2v_wan22_t2v_a14b.toml"
description = "Wan2.2-T2V-A14B t2v baseline (2-expert MoE). Runnable code vendored in models/wan22_t2v_a14b/baseline; weights referenced read-only by absolute path via WAN22_WEIGHTS; diffusers/torch env is the sparse_attn_training .venv."

[baseline.copy]
paths_are_repo_relative = true
include = [
  "models/wan22_t2v_a14b.toml",
  "models/wan22_t2v_a14b/**",
  "models/wan22_t2v_a14b/baseline/**",
  "candidates/wan22_t2v_a14b_baseline.toml",
  "candidates/schema.md",
  "evals/profiles/official_video_t2v_wan22_t2v_a14b.toml",
  "evals/tiers.toml",
  "scripts/launch_candidate.py",
  "scripts/collect_run.py",
  "search/plan_eval.py",
  "techniques/candidate_manifest.py",
]
exclude = [
  ".git/**",
  "**/__pycache__/**",
  "**/*.pyc",
  "runs/**",
  "output/**",
  "workflow/**",
  "search_space/**",
]

[[baseline.reference_only]]
name = "wan22_t2v_a14b_diffusers_weights"
path = "/lustre/fs1/portfolios/nvr/projects/nvr_elm_llm/users/yitongl/pretrained_models/Wan2.2-T2V-A14B-Diffusers"
repo = "Wan-AI/Wan2.2-T2V-A14B-Diffusers"
repo_type = "model"
reason = "~118G Wan2.2 T2V-A14B diffusers weights (MoE: transformer + transformer_2 + vae + UMT5); referenced by absolute path via WAN22_WEIGHTS. Neutral, never copied."

[[baseline.reference_only]]
name = "wan22_diffusers_env"
path = "/lustre/fs1/portfolios/nvr/projects/nvr_elm_llm/users/yitongl/code/sparse_attn_training/.venv"
repo_type = "python_env"
reason = "venv with torch 2.11+cu128 + diffusers 0.37.1 (has WanPipeline + A14B MoE support); used via PYTHON_BIN. Neutral, never copied."

[experiment_defaults]
worktree_mode = "model_baseline_minimal_copy"
experiments_root = "output/experiments"
state_dir = "state"
runs_dir = "runs"
logs_dir = "logs"
caches = ["tmp", "triton", "torch_extensions", "python_deps"]
