# Runs

This directory is for generated run bundles and artifacts.

The launcher creates subdirectories such as:

```text
runs/20260612-063000-cosmos3_baseline/
```

Everything under `runs/` is ignored by git except this README. Keep videos,
logs, frames, reports, Slurm output, and temporary launch files here.
