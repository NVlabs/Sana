schema_version = 1
id = "wan21_t2v_14b"
display_name = "Wan2.1-T2V-14B 720p81 T2V baseline (diffusers WanPipeline, dense)"
legacy_profile = "models/wan21_t2v_14b.toml"
contract_kind = "model_baseline_minimal_copy"

[baseline]
manifest = "candidates/wan21_14b_baseline.toml"
runtime_root = "models/wan21_t2v_14b/baseline"
eval_profile = "evals/profiles/official_video_t2v_wan22_ti2v_5b.toml"
description = "Wan2.1-T2V-14B t2v baseline (dense, no MoE / no transformer_2). Runnable code is vendored in models/wan21_t2v_14b/baseline; weights are referenced read-only through WAN22_WEIGHTS. The optimized candidate adds regional compile, EasyCache, and the released Sol-Attn SM90/SM100 kernel."

[baseline.copy]
paths_are_repo_relative = true
include = [
  "models/wan21_t2v_14b.toml",
  "models/wan21_t2v_14b/**",
  "models/wan21_t2v_14b/baseline/**",
  "candidates/wan21_14b_baseline.toml",
  "candidates/schema.md",
  "evals/profiles/official_video_t2v_wan22_ti2v_5b.toml",
  "evals/tiers.toml",
  "scripts/launch_candidate.py",
  "scripts/collect_run.py",
  "search/plan_eval.py",
  "techniques/candidate_manifest.py",
]
exclude = [
  ".git/**",
  "**/__pycache__/**",
  "**/*.pyc",
  "runs/**",
  "output/**",
  "workflow/**",
  "search_space/**",
]

[[baseline.reference_only]]
name = "wan21_t2v_14b_diffusers_weights"
path = "/lustre/fs1/portfolios/nvr/projects/nvr_elm_llm/users/yitongl/pretrained_models/Wan2.1-T2V-14B-Diffusers"
repo = "Wan-AI/Wan2.1-T2V-14B-Diffusers"
repo_type = "model"
reason = "Wan2.1 T2V-14B diffusers weights (dense: transformer + vae + UMT5); referenced by absolute path via WAN22_WEIGHTS. Neutral, never copied."

[[baseline.reference_only]]
name = "wan22_diffusers_env"
path = "/lustre/fs1/portfolios/nvr/projects/nvr_elm_llm/users/yitongl/code/sparse_attn_training/.venv"
repo_type = "python_env"
reason = "venv with torch 2.11+cu128 + diffusers 0.37.1 (has WanPipeline); used via PYTHON_BIN. Neutral, never copied."

[experiment_defaults]
worktree_mode = "model_baseline_minimal_copy"
experiments_root = "output/experiments"
state_dir = "state"
runs_dir = "runs"
logs_dir = "logs"
caches = ["tmp", "triton", "torch_extensions", "python_deps"]
