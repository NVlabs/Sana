# Golden-output provenance

The source workspace stored `golden_base.mp4` and `golden_refined.mp4` here.
They are generated benchmark artifacts and are intentionally excluded from the
vendored code snapshot (the coordinator globally ignores MP4 files). Recreate
the control with `candidates/lingbot_video_baseline.toml` before running an
aligned quality comparison.
