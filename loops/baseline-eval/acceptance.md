# Acceptance

- Artifact gate passes.
- Official config matches `evals/profiles/official_video_t2v.toml`.
- `outputs/out.mp4` is non-empty.
- `outputs/benchmark.json` exists or timing limitation is documented.
- `outputs/frames/` exists after collection.
- This loop does not require speedup.
