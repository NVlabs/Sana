diff --git a/svg/models/wan/attention.py b/svg/models/wan/attention.py
index 103ded4..668254e 100755
--- a/svg/models/wan/attention.py
+++ b/svg/models/wan/attention.py
@@ -453,11 +453,23 @@ def _pisa_attention_bhsd(
     threshold,
     block_size,
     group_size=None,
     num_warps=None,
     num_stages=None,
+    pisa2_impl="triton",
 ):
+    if backend == "pisa2" and pisa2_impl == "sm100-colmask":
+        from integrations.wan import run
+
+        return run(
+            q,
+            k,
+            v,
+            tau=threshold,
+            block_size=block_size,
+        )
+
     _ensure_pisa_importable()
     if backend == "pisa0":
         from kernels.piecewise_sparse_attn_0th import piecewise_attention
 
         return piecewise_attention(
@@ -628,10 +640,23 @@ def _calibrate_pisa2_threshold_bhsd(q, k, target_density, block_size, group_size
         kv_block_size=int(block_size),
         q_start_offset=0,
     )
 
 
+@torch.compiler.disable
+def _calibrate_pisa2_sm100_tau_bhsd(q, k, v, target_density, block_size):
+    from integrations.wan import calibrate_tau
+
+    return calibrate_tau(
+        q,
+        k,
+        v,
+        target_density=target_density,
+        block_size=block_size,
+    )
+
+
 class WanAttn_PISAAttn_Processor(WanAttn_SVGAttn_Processor2_0):
     pisa_backend = "pisa0"
     pisa_density = 0.15
     pisa_threshold = 1.7
     pisa_block_size = 64
@@ -640,10 +665,11 @@ class WanAttn_PISAAttn_Processor(WanAttn_SVGAttn_Processor2_0):
     pisa2_config_tuned = False
     pisa2_group_size = None
     pisa2_num_warps = None
     pisa2_num_stages = None
     pisa2_tune_result = None
+    pisa2_impl = "triton"
     pisa_reorder_order = "none"
     pisa_reorder_grid_h = 0
     pisa_reorder_grid_w = 0
     pisa_reorder_cache = {}
 
@@ -706,17 +732,26 @@ class WanAttn_PISAAttn_Processor(WanAttn_SVGAttn_Processor2_0):
             and os.getenv("PISA2_AUTO_CALIBRATE", "0") == "1"
             and not cls.pisa2_calibrated
         ):
             if torch.cuda.is_available():
                 torch.cuda.synchronize()
-            result = _calibrate_pisa2_threshold_bhsd(
-                query_in,
-                key_in,
-                target_density=self.pisa_density,
-                block_size=self.pisa_block_size,
-                group_size=cls.pisa2_group_size,
-            )
+            if cls.pisa2_impl == "sm100-colmask":
+                result = _calibrate_pisa2_sm100_tau_bhsd(
+                    query_in,
+                    key_in,
+                    value_in,
+                    target_density=self.pisa_density,
+                    block_size=self.pisa_block_size,
+                )
+            else:
+                result = _calibrate_pisa2_threshold_bhsd(
+                    query_in,
+                    key_in,
+                    target_density=self.pisa_density,
+                    block_size=self.pisa_block_size,
+                    group_size=cls.pisa2_group_size,
+                )
             cls.pisa_threshold = float(result["threshold"])
             cls.pisa2_calibration_result = result
             cls.pisa2_calibrated = True
             logger.info(
                 "WAN PISA2 calibrated threshold={} density={} delta={} target={}",
@@ -730,10 +765,12 @@ class WanAttn_PISAAttn_Processor(WanAttn_SVGAttn_Processor2_0):
         if (
             self.pisa_backend == "pisa2"
             and os.getenv("PISA2_AUTOTUNE_CONFIG", "0") == "1"
             and not cls.pisa2_config_tuned
         ):
+            if cls.pisa2_impl == "sm100-colmask":
+                raise RuntimeError("PISA2_AUTOTUNE_CONFIG is not valid for the fixed SM100 colmask release")
             if torch.cuda.is_available():
                 torch.cuda.synchronize()
             result = _tune_pisa2_config_bhsd(
                 query_in,
                 key_in,
@@ -757,10 +794,11 @@ class WanAttn_PISAAttn_Processor(WanAttn_SVGAttn_Processor2_0):
             threshold=self.pisa_threshold,
             block_size=self.pisa_block_size,
             group_size=cls.pisa2_group_size,
             num_warps=cls.pisa2_num_warps,
             num_stages=cls.pisa2_num_stages,
+            pisa2_impl=cls.pisa2_impl,
         )
         if inv_perm is not None:
             output = output[:, :, inv_perm, :].contiguous()
         return output
 
diff --git a/svg/models/wan/inference.py b/svg/models/wan/inference.py
index d3f9b3d..c2b69cf 100755
--- a/svg/models/wan/inference.py
+++ b/svg/models/wan/inference.py
@@ -39,10 +39,11 @@ def replace_wan_attention(
     zero_step_kmeans_init=False,
     pisa_density=0.15,
     pisa_threshold=1.7,
     pisa_block_size=64,
     pisa_reorder_order="none",
+    pisa2_impl="triton",
 ):
 
     context_length = 0  # This seems to be 0 for I2V in SVG
     num_frame_patches = 1 + num_frames // (pipe.vae_scale_factor_temporal * pipe.transformer.config.patch_size[0])
     mod_value = pipe.vae_scale_factor_spatial * pipe.transformer.config.patch_size[1]
@@ -197,19 +198,21 @@ def replace_wan_attention(
         AttnModule.pisa2_config_tuned = False
         AttnModule.pisa2_tune_result = None
         AttnModule.pisa2_group_size = None
         AttnModule.pisa2_num_warps = None
         AttnModule.pisa2_num_stages = None
+        AttnModule.pisa2_impl = pisa2_impl
         AttnModule.pisa_reorder_order = pisa_reorder_order
         AttnModule.pisa_reorder_grid_h = frame_grid_h
         AttnModule.pisa_reorder_grid_w = frame_grid_w
         AttnModule.pisa_reorder_cache = {}
 
         logger.info(
             f"Configuring WAN {pattern} attention: density={pisa_density}, "
             f"threshold={pisa_threshold}, block_size={pisa_block_size}, "
-            f"reorder={pisa_reorder_order}, grid={frame_grid_h}x{frame_grid_w}"
+            f"reorder={pisa_reorder_order}, grid={frame_grid_h}x{frame_grid_w}, "
+            f"pisa2_impl={pisa2_impl}"
         )
 
         for layer_idx, m in enumerate(pipe.transformer.blocks):
             if hasattr(m.attn1, "processor"):
                 current_processor = AttnModule(layer_idx=layer_idx)
diff --git a/wan_t2v_inference.py b/wan_t2v_inference.py
index 82e8ec7..687f52f 100644
--- a/wan_t2v_inference.py
+++ b/wan_t2v_inference.py
@@ -52,10 +52,17 @@ if __name__ == "__main__":
     parser.add_argument("--kmeans_iter_init", type=int, default=0, help="Number of KMeans iterations for initialization in KMEANS_BLOCK.")
     parser.add_argument("--kmeans_iter_step", type=int, default=0, help="Number of KMeans iterations for other diffusion steps in KMEANS_BLOCK.")
     parser.add_argument("--pisa_density", type=float, default=0.15, help="PISA0 exact block density.")
     parser.add_argument("--pisa_threshold", type=float, default=1.7, help="PISA2 routing threshold.")
     parser.add_argument("--pisa_block_size", type=int, default=64, help="PISA routing block size.")
+    parser.add_argument(
+        "--pisa2_impl",
+        type=str,
+        default="triton",
+        choices=["triton", "sm100-colmask"],
+        help="PISA2 forward implementation; sm100-colmask requires Sol-Attn and B200.",
+    )
     parser.add_argument(
         "--pisa_reorder_order",
         type=str,
         default="none",
         choices=["none", "morton3d", "hwf", "fwh"],
@@ -172,10 +179,11 @@ if __name__ == "__main__":
             pattern=args.pattern,
             pisa_density=args.pisa_density,
             pisa_threshold=args.pisa_threshold,
             pisa_block_size=args.pisa_block_size,
             pisa_reorder_order=args.pisa_reorder_order,
+            pisa2_impl=args.pisa2_impl,
         )
 
     # Print time logger
     for block in pipe.transformer.blocks:
         block.register_forward_hook(print_operator_log_data)
