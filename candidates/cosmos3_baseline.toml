id = "cosmos3_baseline"
kind = "baseline"
purpose = "control"
description = "Cosmos3-Super 64B 1280x720 189f text-to-video baseline; official 35-step config on 4x GB200 with sequence parallelism and every acceleration seam explicitly off. Reference (~130.4s) for optimization."
model_profile = "cosmos3"

[runtime]
root = "models/cosmos3/baseline"

[artifacts]
output_dir = "outputs"
video = "out.mp4"
log = "run.log"
benchmark = "perf.json"
run_config = "run_config.json"
