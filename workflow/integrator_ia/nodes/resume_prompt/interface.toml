name = "resume_prompt"
kind = "orchestration"
owned_by = "integrator_ia"
callable_by = ["workflow/integrator_ia/workflow.py"]
description = "Write a workflow resume prompt for the kernel+PISA+cache integration executor."

inputs = ["resume_target", "resume_reason", "resume_followups"]
outputs = ["goals/<id>/STOP_HOOK_RESUME.md"]
outcomes = ["written", "failed"]
side_effects = ["writes_goal_dir"]
